[project]
name = "frontier-brain"
version = "0.1.0"
description = "Decision Knowledge Graph — autonomous decision memory for Claude Code"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[project.scripts]
decision-engine = "bin.decision_engine:main"
